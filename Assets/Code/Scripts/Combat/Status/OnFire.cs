using System.Collections.Generic;

/*
public class OnFire : Status {

    private DamageEvent damage;

    public OnFire(Unit owner, Unit caster, Ability ability, Dictionary<string, float> data) : base(owner, caster, ability, data) {

    }

    public override void OnCreated() {
        damage = new DamageEvent(Parent, Parent, 50, Ability, DamageFlags.HPLOSS);
        StartIntervalThink(0.5f);

    }

    public override void OnIntervalThink() {
        RotW.ApplyDamage(damage);
    }

    public override void OnDestroy() {
        //GetParent.GetComponent<Image>().color = Color.white;
    }
}*/
