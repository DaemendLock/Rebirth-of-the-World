using UnityEngine;

/*
public class Firefox : Unit {
    public override void Init() {
    }

    public override void OnDamage(AttackEventInstance e) {
        if (e.Target == e.Inflictor && !HasStatus("comburst_status") && e.Value > 0)
            GiveMana(e.Value * 0.005f, (int) AbilityResource.RIGHT);
    }

}*/