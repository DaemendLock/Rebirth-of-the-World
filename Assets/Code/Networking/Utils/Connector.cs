using System.Net.Sockets;

namespace Networking.Utils
{
    public static class Connector
    {
        private const string ip = "127.0.0.1";

        public static bool TryConnectToCombat(int combatPort) { return false; }
    }
}
